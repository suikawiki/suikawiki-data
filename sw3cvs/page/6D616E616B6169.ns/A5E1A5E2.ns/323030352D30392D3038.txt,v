head	1.1;
access;
symbols
	suikawiki3-final:1.1;
locks; strict;
comment	@# @;
expand	@o@;


1.1
date	2005.11.18.02.19.43;	author wakaba;	state Exp;
branches;
next	;


desc
@@


1.1
log
@auto-committed
@
text
@[1]  

The XPath data model module

Interface XPathObject

Interface XPathNodeSet

Interface XPathBoolean
  An XPath boolean value, i.e. true or false.
      The implementations MAY choose to implement the XPath boolean
  data type through the binding native data type.

Interface XPathNumber
  An XPath number, i.e. IEEE 754 double-precision 64-bit value.
      The implementations MAY choose to implement the XPath number
  data type through the binding native data type.

Interface XPathString
  An XPath string.
      The implementations MAY choose to implement the XPath string
  data type through the binding native data type or as the "DOMString" type.

Interface XPathNode
  This interface is intended to be implemented by the nodes in
  the XPath data model.
      Objects that are not XPath data model nodes MAY implement
  this interface if desired.  In such case, the "xpathNodeType"
  attribute MUST be "XPATH_UNKNOWN_NODE".  Any other attribute
  values and how methods should act are implementation dependent.
      Objects implementing this interface MUST also implement
  the "XPathNSResolver" interface.  The "lookupNamespaceURI"
  method resolves a namespace prefix into the namespace URI
  by looking up namespace nodes.  If "null" is specified for the "prefix"
  parameter, the default namespace URI is returned.
      @@@@lookupNamespacePrefix, addNamespaceBinding @@@@
  Definition Group DocumentOrder
    Defined Constants
      DOCUMENT_ORDER_SAME
      DOCUMENT_ORDER_PRECEDING
      DOCUMENT_ORDER_FOLLOWING
      DOCUMENT_ORDER_IMPLEMENTATION_DEPENDENT
  Definition Group XPathNodeType
    An integer indicating which type of node this is.
    Defined Constants
      XPATH_UNKNOWN_NODE
        The node is not an XPath node.
      XPATH_ROOT_NODE
        The node is an XPath root node.
      XPATH_ELEMENT_NODE
        The node is an XPath element node.
      XPATH_TEXT_NODE
        The node is an XPath text node.
      XPATH_ATTRIBUTE_NODE
        The node is an XPath attribute node.
      XPATH_NAMESPACE_NODE
        The node is an XPath namespace node.
      XPATH_PROCESSING_INSTRUCTION_NODE
        The node is an XPath processing instruction node.
      XPATH_COMMENT_NODE
        The node is an XPath comment node.
  Attributes
    stringValue of type XPathString, read-only
      The string-value for the node, if any, or "null".
    xpathChildNodes of type XPathNodeSet, read-only
      A "live" ordered list of child nodes, if the node is either
      a root node or an element node, or "null".
    xpathLocalPart of type XPathString, read-only
      The local part of the node, if any, or "null".
    xpathNamespaceURI of type XPathString (DOM URI), read-only
      The namespace URI of the node, if any, or "null".
    xpathNodeType of type unsigned-short (XPathNodeType), read-only
      The type of the node.
    xpathParentNode of type XPathNode, read-only
      The parent of the node, if any, or "null".
  Methods
    compareDocumentOrder
      Compares the node with another node according to the document order.
      Parameters
        other of type XPathNode
          The node to compare against the node.
      Return Value
        unsigned-short (DocumentOrder)
      Exceptions
        DOMException  NOT_SUPPORTED_ERR: When the compared nodes are from
                      different implementations that do not coordinate to
                      return consistent implementation-specific results.

Interface XPathRootNode
  This interface is implemented by the XPath root nodes.  The objects
  implementing this interface MUST also implement the "XPathNode"
  interface.

Interface XPathElementNode
  This interface is implemented by the XPath element nodes.  The objects
  implementing this interface MUST also implement the "XPathNode"
  interface.
  Attributes
    xpathId of type XPathString, read-only
      The unique identifier of the node, if any, or "null".
          Note.  An element node may have a unique identifier in the XPath
      data model.

Interface XPathAttributeNode
Interface XPathNamespaceNode
Interface XPathProcessingInstructionNode
Interface XPathCommentNode

Interface XPathTextNode

--
The XPath Evaluation Environment module

Interface XPathContext
  Attributes
    xpathContextNode of type XPathNode, read-only
      The context node from XPath expression context.
    contextPosition of type unsigned-long, read-only
      The context position from XPath expression context (function "position ()").
      The context position is always less than or equal to the context size.
    contextSize of type unsigned-long, read-only
      The context size from XPath expression context.
    functionLibrary of type XPathFunctionLibrary
      The XPath function library.
      Exception on setting
        DOMException  NO_MODIFICATION_ALLOWED_ERR: The context is read-only.
    namespaceContext of type XPathNSResolver
      The set of namespace declarations in scope.
      Exception on setting
        DOMException  NO_MODIFICATION_ALLOWED_ERR: The context is read-only.
    variableBindings of type XPathVariableList
      The current set of variable bindings.
      Exception on setting
        DOMException  NO_MODIFICATION_ALLOWED_ERR: The context is read-only.
  Methods
    xpathSetContextNode
      Sets the context node, context position and context size.
      Parameters
        xpathContextNodeArg of type XPathNode
        contextPositionArg of type unsigned-short
        contextSizeArg of type unsigned-short
      No Return Value
      Exceptions
        DOMException  INDEX_SIZE_ERR: The "contextSizeArg" value is less than 
                     the "contextPositionArg" value.

Interface XPathVariableList
  A "live" collection of XPath variable bindings.
  Methods
    getValue
      Returns the variable value.
      Parameters
        namespaceURI of type DOMString (DOM URI)
          The namespace URI of the variable, if any, or "null".
        localPart of type DOMString
          The local part of the variable.
      Return Value
        DOMObject     The variable value associated to the name.
      Exceptions
        DOMException  NOT_FOUND_ERR: The specified variable is not
                      found in this list.
    hasValue
      Returns whether a variable binding is defined or not.
    setValue
      Creates or replaces a variable binding by a new value.

Interface XPathFunctionLibrary
  Methods
    getFunction
      Parameters
        namespaceURI of type DOMString (DOM URI)
        localPart of type DOMString
      Return Value
        XPathFunction  or "null".

Interface XPathFunction

--
The XSLT Data Model module
    This module requires the XPath data model module.

Interface XSLTNode
  The objects implementing "XPathNode" interface MAY also implement
  this interface.  The node objects in the XSLT data model MUST
  implement this interface.
  Definition Group XSLTNodeType
    An integer indicating which type of node this is.
    Defined Constants
      XSLT_UNKNOWN_NODE
        The node is not an XSLT node.
      XSLT_ROOT_NODE
        The node is an XSLT root node.  Note that the "xpathNodeType"
        attribute might be "XPATH_UNKNOWN_NODE" even if the "xsltNodeType"
        attribute is "XSLT_ROOT_NODE", since XSLT 1.1 relaxes the
        restrictions on the children of the root nodes.  All the attributes
        and methods in the XPath data model module MUST treat the nodes
        whose "xpathNodeType" is "XPATH_UNKNOWN_NODE" and whose
        "xsltNodeType" is "XSLT_ROOT_NODE" as if they are XPath element
        nodes with regard to their children and as if they are XPath
        root nodes for the other regards.
      XSLT_ELEMENT_NODE
        The node is an XSLT element node.
      XSLT_TEXT_NODE
        The node is an XSLT text node.
      XSLT_ATTRIBUTE_NODE
        The node is an XSLT attribute node.
      XSLT_NAMESPACE_NODE
        The node is an XSLT namespace node.
      XSLT_PROCESSING_INSTRUCTION_NODE
        The node is an XSLT processing instruction node.
      XSLT_COMMENT_NODE
        The node is an XSLT comment node.
      XSLT_UNPARSED_ENTITY_NODE
        The node is an unparsed entity.
            Note.  This kind of objects are not nodes in the XSLT
        data model.
  Attributes
    xsltBaseURI of type XPathString (DOM URI), read-only
      The base URI of the node.  It is the [declaration base URI] for 
      the unparsed entities.
    xsltNodeType of type unsigned-short (XSLTNodeType), read-only
      The type of the node in the XSLT data model.
  Methods
    mergeAdjacentTextNodes
      Merges adjacent text nodes.
          Note.  The operation should be done by modifying the string-value of the
      first text node and removing any other adjacent text nodes, unless
      the first text node is read-only.
      No Parameter
      No Return Value
      Exceptions
        DOMException  NO_MODIFICATION_ALLOWED_ERR: If the parent node of
                      the text nodes is read-only.

Interface XSLTRootNode
  The root nodes in the XSLT data model MUST implement this interface.
  The "XSLTRootNode" objects MUST also implement the "XSLTNode"
  interface.
  Methods
    xsltNamespaceFixup
      Adds namespace nodes so that it satisfies constraints on them.
      No Parameter
      No Return Value
      Exceptions
        DOMException  NO_MODIFICATION_ALLOWED_ERR: If the parent node of
                      the stripping text node is read-only.
                          Note.  Some namespace node might be added
                      before the exception is thrown.


Interface XSLTElementNode
  The element nodes in the XSLT data model MUST implement this interface.
  The "XSLTElementNode" objects MUST also implement the "XSLTNode"
  interface and the "XPathElementNode" interface.
  Attributes
    xmlspace of type XPathString
      This attribute _reflect_s the "xml:space" attribute of the element.
      Exception on setting
        DOMException  NO_MODIFICATION_ALLOWED_ERR: If the node is read-only.

Interface XSLTNamespaceNode
  The namespace nodes in the XSLT data model MUST implement this
  interface and the "XSLTNode" interface.
  Attributes
    isAllowable of type boolean, read-only
      Whether the node is allowable or not.

Interface XSLTUnparsedEntity
  In the XSLT data model, the root node has a mapping that gives
  the URI for each unparsed entity declared in the document's DTD.
  The unparsed entity objects MUST implement this interface.  Although
  unparsed entities are not nodes in the XSLT data model, the objects
  implementing this interface MUST also implement the "XSLTNode"
  interface for convinience.
  Attributes
    uri of type XPathString (DOM URI), read-only
      The absolute URI for the unparsed entity.

Interface XSLTResultTreeFragment

Interface XSLTExternalObject
  @@@@ XSLT 1.0 only implementation need not @@@@

--
The XSLT Stylesheet Module

Interface NameTestList extends NameList
  A "NameTestList" object represents a set of "NameTest"s.  For each
  item, the "namespaceURI" is the namespace URI specified by the "NameTest"
  or "null" if the "NameTest" has no prefix and the "localName" is
  the local part in the "NameTest" or "null" if it is "*".
  @@@@ NO_MODIFICATION_ALLOWED_ERR @@@@

Interface XSLTStylesheetNode
  The node objects in XSLT stylesheet MUST implement this interface.
  The objects implementing this interface MUST also implement the
  "XSLTNode" interface.
  Definition Group XSLTStylesheetNodeType
    Defined Constants
      XSLT_STYLESHEET_ERROR_NODE
        The node is errorous with regard to its context.
            Note.  It might be ignored in forwards-compatible processing mode.
      XSLT_STYLESHEET_STANDARD_NODE
        The node is a standardized attribute, the root element
        whose type belongs to the XSLT namespace, the
        root node, a text node, a namespace node, or an unparsed entity.
      XSLT_STYLESHEET_EXTENSION_NODE
        The node is an extension attribute.
      XSLT_STYLESHEET_TOP_LEVEL_STANDARD_NODE
        The node is a standardized top-level element.
      XSLT_STYLESHEET_TOP_LEVEL_EXTENSION_NODE
      XSLT_STYLESHEET_INSTRUCTION_STANDARD_NODE
      XSLT_STYLESHEET_INSTRUCTION_EXTENSION_NODE
      XSLT_STYLESHEET_LITERAL_NODE
        The node is a literal result node.
      XSLT_STYLESHEET_IGNORED_NODE
        The node is ignored.  It might be a comment or processing
        instruction node.
  Attributes
    xsltStylesheetNodeType of type unsigned-short (XSLTStylesheetNodeType), read-only
      The role of the node in the stylesheet.

Interface XSLTStylesheetRootNode extends GetFeature, XPathNode, XSLTNode, XSLTRootNode, XSLTStylesheetNode
  The root node of the XSLT stylesheet MUST implement this interface.
  Methods
    stripWhitespace
      Strips the text node that only contains whitespace characters
      and they are not specified to preserve.
      No Parameter
      Exception
        DOMException  NO_MODIFICATION_ALLOWED_ERR: If the parent node of
                      the stripping text node is read-only.
                          Note.  Some text node might be stripped
                      before the exception is thrown.

Interface XSLTStylesheetElementNode
  This interface MUST be implemented by the element nodes in the
  XSLT stylesheet document.  The objects implementing this interface
  MUST also implement the "XSLTStylesheetNode" interface and
  the "XSLTElementNode" interface.

Interface XSLTStylesheetElementNodeRoot
  This interface is implemented by the outermost XSLT element node object
  in the stylesheet.  The object implementing this interface MUST also
  implement ths "XSLTStylesheetElementNode" interface.
      Note.  XSLT stylesheets might be embeded in another document.
  Although the outermost XSLT element might not be the root element
  in the document, such element node must implement this interface.
  Attributes
    xsltImportChildNodes of type XPathNodeSet, read-only
      A "live" list of import children.  Each "item" is the root
      element node corresponding to an "xslt:import" element in this document
      and MUST implement the "XSLTStylesheetElementNodeRoot" interface.
      Exception on retrieve
        DOMException  NOT_SUPPORTED_ERR: @@@@load@@@@
    xsltVersion of type XPathString
      This attribute _reflect_s the "version" attribute in the "null" namespace
      of the element if the namespace URI of the element is the XSLT namespace URI or
      the "xslt:version" attribute of the element otherwise.
      Exception on setting
        DOMException  NO_MODIFICATION_ALLOWED_ERR: If the node is read-only.
  Methods
    getXSLTImportTreeNodes
      A "live" list of nodes in the import tree rooted by this element.
      If the stylesheet contains no "xslt:import" element, the root
      element of the stylesheet itself is the only node in the list.
      The nodes are ordered by import precedence (lower first).
          Note.  There might be more than one occurences of the same node
      if the same stylesheet is imported more than once.
      No Parameter
      Return Value
        XPathNodeSet
      Exceptions
        DOMException  NOT_SUPPORTED_ERR: @@@@load@@@@

Interface XSLTStylesheetElement extends GetFeature, XPathNode, XPathElementNode, XSLTNode, XSLTElementNode, XSLTStylesheetNode, XSLTStylesheetElementNode, XSLTStylesheetElementNodeRoot, XSLTStylesheetElementNodeResult
  The "xslt:stylesheet" or "xslt:transform" element in XSLT stylesheet
  MUST implement this interface.
  Attributes
    id of type XPathString
      This attribute _reflect_s the "id" attribute of the element
      in the "null" namespace.
          Note.  If the attribute node for the "id" attribute in
      the XSLT stylesheet implements the "Attr" interface, the
      "Attr.isId" attribute MUST return "true" by default.
      Exception on setting
        DOMException  NO_MODIFICATION_ALLOWED_ERR: If the node is read-only.
  Methods
    mergeAttributeSets
      Merges the attribute sets of the same name in the stylesheet.
          Note.  Attributes sets in the included or imported stylesheets
      are not the subject of the operation.
      Exceptions
        DOMException  NO_MODIFICATION_ALLOWED_ERR: If the parent node of
                      an attribute set node or its children is read-only.
                          Note.  Some nodes might be moved, inserted or removed
                      before the exception is thrown.  Even in such case,
                      the semantics of overall stylesheet ought to be preserved.

Interface XSLTStylesheetElementNodeReference
  The "xslt:import" and "xslt:include" elements in XSLT stylesheet
  MUST implement this interface.
      The implementation MAY provide access to stylesheets referenced
  from those elements.  Such an implementation MUST return "true"
  in the method "GetFeature.hasFeature" giving feature
  "@@@@NAMESPACE@@@@referencedStylesheet" version "1.0".
  Const Group XSLTExternalStylesheetStatus
    XSLT_INCLUDE_NOT_LOADED of type unsigned-short
    XSLT_INCLUDE_LOADED of type unsigned-short
    XSLT_INCLUDE_ERROR of type unsigned-short
      Retrieve error or recursive reference or ...
  Attributes
    href of type XPathString (DOM URI)
      This DOM attribute "reflect"s the XSLT attribute "href".
      Setting attribute value makes the new stylesheet loaded
      and the "xsltReferencedDocument" attribute replaced.
      Exception on setting
        DOMException  NO_MODIFICATION_ALLOWED_ERR: If the node is read-only.
    xsltReferencedDocument of type XSLTStylesheetRootNode, read-only
      The included or imported XSLT template, if loaded, or "null".

Interface XSLTIncludeElement extends GetFeature, XPathNode, XPathElementNode, XSLTNode, XSLTElementNode, XSLTStylesheetNode, XSLTStylesheetElementNode, XSLTStylesheetElementNodeReference, XSLTStylesheetInstructionElementNode
  The "xslt:include" element in XSLT stylesheet MUST implement this interface.
  Methods
    merge
      Replaces this element by a set of deep clones of all top-level
      elements in the "xsltReferencedDocument".
      @@@@ exclude-result-prefixes, extension-element-prefix @@@@
      No Parameter
      No Return Value
      Exceptions
        DOMException   NO_MODIFICATION_ALLOWED_ERR: If the document is read-only.
        XSLTException  @@@@not loaded@@@@

Interface XSLTImportElement extends GetFeature, XPathNode, XPathElementNode, XSLTNode, XSLTElementNode, XSLTStylesheetNode, XSLTStylesheetElementNode, XSLTStylesheetElementNodeReference

Interface XSLTStylesheetElementElements
  This interface MUST be implemented by XSLT element which might have
  "elements" attribute whose value is a whitespace-separeted list of "NameTest"s.
  Attributes
    xsltElements of type NameTestList, read-only
      This attribute _reflect_s the "elements" attribute.

Interface XSLTPreserveSpaceElement extends ..., XSLTStylesheetTopLevelElementNode, XSLTStylesheetElementNodeElements

Interface XSLTStripSpaceElement extends ..., XSLTStylesheetTopLevelElementNode, XSLTStylesheetElementNodeElements

Interface XSLTTemplateElement extends XPathNode, XPathElementNode, XSLTNode, XSLTElementNode, XSLTStylesheetNode, XSLTStylesheetElementNode, XSLTStylesheetTopLevelElementNode, XSLTStylesheetElementNodeTemplate, XSLTStylesheetElementNodeMode, XSLTStylesheetElementNodeName, XSLTStylesheeTopLevelElementNode
  Attributes
    match of type XPathString
      This attribute _reflect_s the "match" attribute.
    priority of type XPathNumber
      This attribute _reflect_s the "priority" attribute.

Interface XSLTStylesheetTopLevelElementNode
  Top-level element.

Interface XSLTStylesheetElementNodeTemplate
  Element whose content is a template.

Interface XSLTStylesheetElementNodeSelect
  Attributes
    xsltSelect of type XPathString
      This attribute _reflect_s the "select" attribute.

Interface XSLTStylesheetElementNodeMode

Interface XSLTStylesheetInstructionElementNode

Interface XSLTCallTemplateElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeWithParam, XSLTStylesheetElementNodeName

Interface XSLTApplyTemplatesElement extends ..., XSLTStylesheetElementNodeSelect, XSLTStylesheetElementNodeMode, XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeSort, XSLTStylesheetElementNodeWithParam
  Attributes
    modeLocalPart of type XPathString, read-only
    modeNamespaceURI of type XPathString (DOM URI), read-only
  Methods
    setMode
      Parameters
        modeNamespaceURI of type XPathString (DOM URI)
        modeLocalPart of type XPathString

Interface XSLTStylesheetElementNodeWithParam

Interface XSLTStylesheetElementNodeSort

Interface XSLTApplyImportsElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeWithParam

Interface XSLTStylesheetElementNodeName
  Attributes
    nameLocalPart of type XPathString
    nameNamespaceURI of type XPathString (DOM URI)
  Methods
    setName

Interface XSLTStylesheetElementNodeResult
  Attributes
    excludedNamespace of type @@@@
      The unordered list of namespace URIs of excluded namespaces.
      It reflects the value of the "exclude-result-prefixes" or
      "xslt:exclude-result-prefixes" attribute.
      @@@@ prefix not defined err @@@@
    extensionNamespace of type @@@@
  Methods
    isExcludedNamespace
      Whether a namespace is an excluded namespace or not.
    isExtensionNamespace

Interface XSLTLiteralResultElement
  Also "XSLTStylesheetElementNodeResult", XSLTStylesheetElementNodeAttribute.

Interface XSLTNamespaceAliasElement extends ..., XSLTStylesheetTopLevelElementNode
  Attributes
    resultNamespaceURI of type XPathString
      This attribute "reflect"s the "result-prefix" attribute.
    stylesheetNamespaceURI of type XPathString
      This attribute "reflect"s the "stylesheet-prefix" attribute.

Interface XSLTElementElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeAttribute, XSLTStylesheetElementNodeTemplate
  Attributes
    name of type XPathString
    namespace of type XPathString

Interface XSLTAttributeElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeCharTemplate
  Attributes
    name of type XPathString
    namespace of type XPathString

Interface XSLTAttributeSetElement extends ..., XSLTStylesheetTopLevelElementNode, XSLTStylesheetElementNodeName, XSLTStylesheetElementNodeAttribute

Interface XSLTStylesheetElementNodeAttribute
  Attributes
    useAttributeSets of type NameList, read-only
      This attribute _reflect_s the "use-attribute-sets" or
      "xslt:use-attribute-sets" attribute.

Interface XSLTCopyElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeAttribute, XSLTStylesheetElementNodeTemplate

Interface XSLTTextElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeText

Interface XSLTCommentElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeCommentTemplate

Interface XSLTProcessingInstructionElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeCharTemplate
  Attributes
    name of type XPathString

Interface XSLTStylesheetElementNodeCharTemplate

Interface XSLTValueOfElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeSelect, XSLTStylesheetElementNodeText

Interface XSLTStylesheetAttributeNode
  MUST "XSLTStylesheetNode" and "XSLTAttributeNode".
  Attributes
    isAttributeValueTemplate of type boolean, read-only
      Whether the attribute value is interpreted as an attribute
      value template or not.

Interface XSLTNumberElement extends ..., XSLTStylesheetInstructionElementNode
  Attributes
    count of type XPathString
      This attribute _reflect_s the "count" attribute.
    from of type XPathString
      This attribute _reflect_s the "from" attribute.
    groupingSeparator of type XPathString
      This attribute _reflect_s the "grouping-separator" attribute.
    level of type XPathString
      This attribute _reflect_s the "level" attribute.
    letterValue of type XPathString
      This attribute _reflect_s the "letter-value" attribute.
    value of type XPathString
      This attribute _reflect_s the "value" attribute.

Interface XSLTForEachElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeSelect, XSLTStylesheetElementNodeTemplate, XSLTStylesheetElementNodeSort

Interface XSLTIfElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeTemplate
  Attributes
    test of type XPathString
      This attribute _reflect_s the "test" attribute.

Interface XSLTChooseElement extends ..., XSLTStylesheetInstructionElementNode
  Attributes
    otherwise of type XSLTOtherwiseElement
      The "xslt:otherwise" child element, if any, or "null" otherwise.
      Setting this attribute remove all child elements whose element type
      is "xslt:otherwise" and appends the new element.
    whenLength of type unsigned-long, read-only
      The number of the "xslt:when" child elements.
  Methods
    getWhenByIndex
      Returns a "xslt:when" child element by its index.
      Parameter
        index of type unsigned-long
          The ordinal index of the element.  The first "xslt:when"
          element is referred by "0".
      Return Value
        XSLTWhenElement
      Exceptions
        DOMException  INDEX_SIZE_ERR: 

Interface XSLTWhenElement extends ..., XSLTStylesheetElementNodeTemplate
  Attributes
    test of type XPathString
      This attribute _reflect_s the "test" attribute.

Interface XSLTOtherwiseElement extends ..., XSLTStylesheetElementNodeTemplate

Interface XSLTSortElement extends ..., XSLTStylesheetElementNodeSelect
  Attributes
    caseOrder of type XPathString
    dataType of type XPathString
    lang of type XPathString
    order of type XPathString

Interface XSLTVariableElement extends ..., XSLTStylesheetElementNodeName, XSLTStylesheetElementNodeSelect, XSLTStylesheetElementNodeTemplate, XSLTStylesheetInstructionElementNode, XSLTStylesheetTopLevelElementNode

Interface XSLTParamElement extends ..., XSLTStylesheetElementNodeName, XSLTStylesheetElementNodeSelect, XSLTStylesheetElementNodeTemplate, XSLTStylesheetTopLevelElementNode

Interface XSLTCopyOfElement extends ..., XSLTStylesheetInstructionElementNode,XSLTStylesheetElementNodeSelect
  "xslt:copy-of".

Interface XSLTWithParamElement extends ..., XSLTStylesheetElementNodeName, XSLTStylesheetElementNodeSelect, XSLTStylesheetElementNodeTemplate
  "xslt:with-param".

Interface XSLTKeyElement extends ..., XSLTStylesheetTopLevelElementNode, XSLTStylesheetElementNodeName
  "xslt:key".
  Attributes
    match of type XPathString
    use of type XPathString

Interface XSLTDecimalFormatElement extends ..., XSLTStylesheetTopLevelElementNode, XSLTStylesheetElementNodeName
  Attributes
    decimalSeparator of type XPathString
    digit of type XPathString
    groupingSeparator of type XPathString
    infinity of type XPathString
    minusSign of type XPathString
    patternSeparator of type XPathString
    percent of type XPathString
    perMille of type XPathString
    xsltNaN of type XPathString
    zeroDigit of type XPathString

Interface XSLTMessageElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeTemplate
  Attributes
    terminate of type XPathBoolean

Interface XSLTScriptElement
  @@@@ XSLT 1.0 only implementation need not @@@@
  Attributes
    archive of type DOMStringList@@@@
    implementsNamespaceURI of type XPathString
    languageLocalPart of type XPathString
    languageNamespaceURI of type XPathString
    src of type XPathString

Interface XSLTFallbackElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeTemplate

Interface XSLTOutputElement extends ..., XSLTStylesheetTopLevelElementNode, XSLTStylesheetElementNodeOutput

Interface XSLTStylesheetElementNodeOutput
  Attributes
    cdataSectionElements of type XPathString
    doctypePublic of type XPathString
    doctypeSystem of type XPathString
    encoding of type XPathString
    indent of type XPathString
    mediaType of type XPathString
    methodLocalPart of type XPathString
    methodNamespaceURI of type XPathString
    standalone of type XPathString
    omitXMLDeclaration of type XPathString
    version of type XPathString

Interface XSLTStylesheetElementNodeText
  Attributes
    disableOutputEscaping of type XPathBoolean

Interface XSLTDocumentElement extends ..., XSLTStylesheetInstructionElementNode, XSLTStylesheetElementNodeTemplate, XSLTStylesheetElementNodeOutput
  @@@@ XSLT 1.0 only need not @@@@
  Attributes
    href of type XPathString

--
The XSLT Processor Module

Interface XSLTImplementation
  The objects implementing this interface MUST also implement
  the "DOMMinimumImplementation" interface.
  Definition Group XSLTProcessMode
    Integers indicating the mode in which the stylesheet will be processed.
    Defined Constants
      XSLT_PROCESS_MODE_1_0 of type unsigned-short
        The stylesheet is processed as XSLT 1.0.
      XSLT_PROCESS_MODE_1_0_COMPAT of type unsigned-short
        The stylesheet is processed in forwards-compatible mode
        as defined in XSLT 1.0.
      XSLT_PROCESS_MODE_1_1 of type unsigned-short
        The stylesheet is processed as XSLT 1.1.
      XSLT_PROCESS_MODE_1_1_COMPAT of type unsigned-short
        The stylesheet is processed in forwards-compatible mode
        as defined in XSLT 1.1.
  Methods
    getProcessMode
      Returns what process mode will be enabled by the implementation
      to process a stylesheet.
      Parameters
        ver of type XPathString
          A version number as specified in the "version" or "xslt:version"
          attribute of the root element of the stylesheets.
      Return Value
        unsigned-short (XSLTProcessMode)  The process mode.

Interface XSLTContext
  The "XSLTContext" objects provide the context information
  in which XSLT template is processed.
      The "XSLTContext" objects MUST also implement the "XPathContext"
  interface.
  Attributes
    contextNode of type Node, read-only
      The context node from XPath expression context.
    contextPosition of type unsigned-long, read-only
      The context position from XPath expression context (function "position ()").
    contextSize of type unsigned-long, read-only
      The context size from XPath expression context.
    currentNode of type Node, read-only
      The current node from XSLT context (function "current ()").
    ownerDocument of type Document, read-only
      The document to be used for creating nodes.
  Methods
    stringValue
      Returns the string value of the "Node" passed in.
      Parameters
        n of type Node
      Return Value
        XPathString
    systemProperty
      Return the XSLT system property value.
      Parameters
        namespaceURI of type XPathString
        localName of type XPathString
      Return Value
        Object    

Interface XSLTCurrentContext extends XSLTContext
  Attributes
    xsltCurrentNode of type XSLTNode, read-only
      The current node.
    xsltCurrentNodeList of type XPathNodeSet, read-only
      The current node list.
    xsltCurrentTemplateRule of type XSLTTemplateRule
      The current template rule.
  Methods
    setXSLTCurrentNodeList
      Sets "xsltCurrentNode" and "xsltCurrentNodeList" attributes.
      Parameters
        xsltCurrentNodeArg of type XSLTNode
        xsltCurrentNodeListArg of type XPathNOdeSet
      No Return Value
      Exceptions
        DOMException  NOT_FOUND_ERR: The "xsltCurrentNodeArg" is not
                      part of the "xsltCurrentNOdeListArg".

Interface XSLTStylesheet
  Attributes
    xsltImportChildStylesheets of type XSLTStylesheetList, read-only
  Methods
    @@@@getKey, getDecimalFormat @@@@
    @@@@ result document tree @@@@
    getResultNamespaceURI
      "xslt:namespace-alias" or the original namespace URI.
      Parameters
        stylesheetNamespaceURI of type XPathString
      Return Value
        XPathString
      No Exception
      Errors
        @@@@ prefix not defined @@@@
        @@@@ multiple alias @@@@
    getTemplateRuleByName
      Parameters
        namespaceURI of type XPathString
        localPart of type XPathString
      Return Value
        XSLTTemplateRule
      No Exception
      Error
        @@@@ multiple templates @@@@
    getXSLTImportTreeNodes
      Return Value
        XSLTStylesheetList
    processNodeSet
      Processes a set of source nodes and append the result tree
      fragments in order.
      Parameters
        srcNodeList of type XPathNodeSet
          The set of source nodes to process.
        resultParent of type XSLTNode
          The node in the result tree to which the results are appended.
      Return Value
        XSLTNode  The "resultParent" node is returned.
      Exceptions
        DOMException  NO_MODIFICATION_ALLOWED_ERR: The "resultParent" node is
                      read-only.
                      HIERARCHY_REQUEST_ERR: An attempt is made to append
                      to "resultParent" a child node whose type is not allowed.
                          Note.  Implementations MAY choose to report
                      errors instead of throwing this kind of exceptions.
      Error
        XSLT process errors might be reported.
    processTree
      Processes a node and constructs a result tree.
      Parameters
        srcNode of type XSLTRootNode
          The root node of the source tree to process.
      Return Value
        XSLTRootNode  The root node of the result tree.
      No Exception
      Error
        XSLT process errors might be reported.
    selectTemplateRuleByNode
      Selects a template rule to process a node.
      Parameters
        srcNode of type XSLTNode
          The source node to process.
        modeNamespaceURI of type XPathString (DOM URI)
          The namespace URI of the mode, if any, or "null".
          If the "modeLocalPart" is "null", the "modeNamespaceURI" is
          ignored.
        modeLocalPart of type XPathString
          The local part of the mode or "null" if the default mode.
      Return Value
        XSLTTemplateRule  The matching template rule.
                              Note.  Since there are built-in template rules,
                          the result might not be "null".
      No Exception
      Error
        @@@@ multiple template error @@@@
          Severity: error
          If an element type matches more than one "xslt:template"
          and they has same priority.
          If recovered: Last element.
        @@@@ neither @@name or @@match template @@@@
    stripWhitespace
      Strips the text node that only contains whitespace characters
      and they are not specified to preserve.
          For the nodes that have no children, including the text nodes
      themselves, this method makes no change.
      Parameters
        node of type XSLTNode
          The node the subtree rooted by it is to be whitespace-stripped.
      Exception
        DOMException  NO_MODIFICATION_ALLOWED_ERR: If the parent node of
                      the stripping text node is read-only.
                          Note.  Some text node might be stripped
                      before the exception is thrown.
      Error
        @@@@namespace@@@@strip-or-non-strip-err @@@@better name@@@@
          Severity: error
          If an element type matches more than one "xslt:strip-space" and
          "xslt:preserve-space" and they has same priority.
          If recovered: Last element, leftmost in attribute.
    @@@@getVariable ??

Interface XSLTStylesheetList

Interface XSLTNamedItem
  Attributes
    localPart of type XPathString, read-only
    namespaceURI of type XPathString, read-only
  Methods
    setName

Interface XSLTTemplateRule extends XSLTNamedItem
  Attribute
    attributePriority of type float
      The priority of the template as specified in the "priority"
      attribute.
      @@@@ Is "float" enough to represent "Number"? @@@@
    defaultPriority of type float
      The default priority of the template rule.
    matchPattern of type XSLTPattern
      The pattern associated to this template.  If this is a named
      template, "null".
    modeLocalPart of type XPathString, read-only
    modeNamespaceURI of type XPathString, read-only
  Methods
    setMode
    @@@@getVariable?? getParam??

Interface XSLTVariable extends XSLTNamedItem
  Attributes
    value of type XPathObject
      The bound value of the variable.

--
The XSLT Expression module

@@@@ XPath expression and pattern @@@@
@@@@ function not defined error @@@@

Interface XSLTPattern
  Methods
    hasVariableReference
      Returns whether the pattern contain a "VariableReference" or not.
      No Parameter
      Return Value
        boolean  Whether the pattern contains a variable reference or not.
      No Exception

Interface XSLTNodeMatch
  This interface provides a set of methods to test whether a node
  satisfies the conditions shown as expressions.
      The object implementing this interface MUST also implement
  the "XSLTNode" interface.
  Methods
    matchPattern
      Tests whether the node satisfies the conditions matches
      the pattern.
      Parameters
        pattern of type XSLTPattern
          The pattern to test.
      Return Value
        boolean  If the node matches the "pattern", "true", and otherwise, "false".
      No Exception

Interface XSLTFunctionLibrary extends XPathCoreFunctionLibrary
  Methods
    current
      Parameters
        context of XSLTCurrentContext
      Return Value
        XSLTNodeSet
    document
      Provides accesses to XML documents other than the main source document.
      Parameters
        context of XSLTCurrentContext
        type XPathObject
        type XPathNodeSet
      Return Value
        XPathNodeSet
    elementAvailable
      Parameters
        context of type XSLTCurrentContext
        type XPathString
      Return Value
        XPathBoolean
    formatNumber
      Parameters
        context of type XSLTCurrentContext
        type XPathNumber
        type XPathString
        type XPathString
      Return Value
        XPathString
    functionAvailable
      Parameters
        context of type XSLTCurrentContext
        type XPathString
      Return Value
        XPathBoolean
    generateId
      Parameters
        context of type XSLTCurrentContext
        type XPathNodeSet
      Return Value
        XPathString
    key
      Parameters
        context of XSLTCurrentContext
        type XPathString
        type XPathObject
      Return Value
        XPathNodeSet
    systemProperty
      Parameters
        context of type XSLTCurrentContext
        type XPathString
    unparsedEntityURI
      Parameters
        context of type XSLTCurrentContext
        type XPathString
@
