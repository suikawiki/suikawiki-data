[FIG(quote)[
[FIGCAPTION[
[1] [CITE[Web Security Context: User Interface Guidelines]]
([TIME[2010-08-04 20:09:50 +09:00]] 版)
<http://www.w3.org/TR/wsc-ui/#typesoftls>
]FIGCAPTION]

> '''['''Definition: An HTTP transaction is strongly TLS-protected if it is TLS-protected, an https URL was used, strong TLS algorithms were negotiated for both confidentiality and integrity protection, and at least one of the following conditions is true:''']'''
> the server used a validated certificate that matches the dereferenced URI; or
> the server used a self-signed certificate that was pinned to the destination; or
> the server used a certificate chain leading to an untrusted root certificate that was pinned to the destination.

]FIG]
