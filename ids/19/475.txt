[1] [CODE(perl)[[[Text::MeCab]]]] で

> Failed to create mecab instance 

エラーが出ると原因は色々あってエスパーするしかないみたいです。

[2] [CODE(perl)[[[new]]]] メソッドを呼ぶときに [CODE[userdic]] で指定したファイルが存在していないと >>1
のエラーが出るみたいです。